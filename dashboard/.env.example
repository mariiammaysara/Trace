# Copy to .env.local (gitignored) to override. Defaults to
# http://localhost:8000 in src/lib/api.ts if unset -- matches plain
# `uvicorn api.app:app`'s own default port.
VITE_API_BASE_URL=http://localhost:8000
